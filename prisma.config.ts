// This file was commented out because it requires Prisma 6+, 
// but the project is currently using Prisma 5.x.
// Deleting or commenting this file fixes the Vercel build error.

/*
import "dotenv/config";
import { defineConfig } from "prisma/config";

export default defineConfig({
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
  },
  datasource: {
    url: process.env["DATABASE_URL"],
  },
});
*/
export {}
